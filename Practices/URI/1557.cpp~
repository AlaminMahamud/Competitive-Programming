#include <bits/stdc++.h>
using namespace std;

int main(){
  int A, B, C;
  while(cin >> A && A){
    cin >> B >> C;
    cout << static_cast<int>(sqrt( (A*B)/(C/100.0) )) << endl;
  }
  
  return 0;
}
