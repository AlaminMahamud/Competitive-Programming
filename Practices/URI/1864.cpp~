#include <bits/stdc++.h>
using namespace std;

int main(){
  int n;
  cin >> n;
  int a[n];
  int d, min = 10000000, mini = 0;
  for(int i = 0; i < n; i++){
    cin >> d;
    if(d < min) {
      min = d;
      mini = i+1;
    }
  }
  cout << mini << endl;
  return 0;
}
