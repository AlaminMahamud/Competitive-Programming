#include <bits/stdc++.h>
#define REP(i,n) for(int i = 0; i < n; i++)
#define valid(nx,ny,row,col) nx >= 0 && 
using namespace std;

int row, col;

int main(){
  cin >> row >> col;
  while(cin){
    int X, Y;
    cin >> X >> Y;
    char D;
    cin >> D;
    string S;
    cin >> S;

    traverse();
    
  }

  
  
  return 0;
}
