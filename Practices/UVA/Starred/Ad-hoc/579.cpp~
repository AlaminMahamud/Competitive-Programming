#include <bits/stdc++.h>
using namespace std;

int main(){

  int hh;
  int mm;
  string s;
  while(cin>>s){
    if(s[1] == ':') {
      hh = s[0] - '0';      
    }else{
      hh = (s[0] - '0') * 10 + (s[1] - '0');
    }
  }
  return 0;
}
