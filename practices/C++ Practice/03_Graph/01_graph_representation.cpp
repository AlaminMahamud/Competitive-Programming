// Adjacency Matrix 
// 2D Array = n*N
// A[i,j] = 1 if i is connected to j
// A[i,j] = 0 if i is not connected to j 


// undirected graph
// weighted edges

// and they are connected 

// we will put some weight on graph
// we will put the weight here

// instead of one we will put the weight of the graph
// if node is not connected to another then assume the weight is INFINITY

int adjMat[n][n];
int adjMat[4][4];
adjMat[2][1];
