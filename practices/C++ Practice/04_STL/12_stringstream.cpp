#include<iostream>
#include<map>

using namespace std;

int main()
{
    char line[1000];
    while(gets(line)){
        stringstream ss(line);
        while(ss>>num)
            v.push_back(num);
        sort(v.begin(),v.end());
        // Print Routine
    }
    return 0;
}