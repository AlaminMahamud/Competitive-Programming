#include <iostream>
#include <cstdio>
#include <string>

using namespace std;

int main(){
    int result;
    string s;

    // scan each line
    getline(cin,s);
    printf("%s",s);
    // split them for space
    // and convert them from string to int
    // and return the result.
}