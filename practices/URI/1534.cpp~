#include <bits/stdc++.h>
using namespace std;

int main(){
  int n;

  while(cin >> n){
    int M[n][n];
    bool B[n][n];
    memset(M, 0, sizeof(M));
    meset(B, false, sizeof(B));
    for(int i = 0; i < n; i++){
      M[i][i] = 1;
      B[i][i] = true;
    }
      
  }

  return 0;
}
