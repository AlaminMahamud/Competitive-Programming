// COPYRIGHT 2017_RED

#include <cstdio>

int main(int argc, char *argv[]) {
  double a, b, c;
  scanf("%lf %lf %lf", &a, &b, &c);
  printf("MEDIA = %.1lf\n", ((a * 2.0 + b * 3.0 + c * 5.0) / 10.0));
  return 0;
}
