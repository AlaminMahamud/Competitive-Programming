#include <cstdio>

int main(){
  while(1){

    int A;
    double B, C;
    int D;

    cin >> A >> B >> C >> D;

    if(A == -1) break;
    
    int M[D];

    for(int i = 0; i < D; i++){
      double DD;
      cin >> DD;
      M[i] = DD;
    }
    
    double car = C + B;
    double loan = C;
    int months = 0;

    double depreciation_rate = -1;
    
    for(int i = 0; loan > car ; i++)
      {
	
	loan -= B;
	car -= 
      }
    
  }

}
